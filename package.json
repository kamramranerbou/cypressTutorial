{
  "name": "cci",
  "version": "1.0.0",
  "description": "CCI - Chatbot Control Interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "delete:reports": "rm -r cypress/results/* && rm -r ./output.html || true",
    "prereport": "npm run delete:reports",
    "mochawesome:merge": "npx mochawesome-merge \"cypress/results/mochawesome/*.json\", > mochawesome.json && npx marge mochawesome.json",
    "merge": "mochawesome-merge cypress/results/mochawesome/*.json > cypress/results/mochawesome/output.json",
    "generateMocha_Report": "marge cypress/results/mochawesome/output.json --reportDir ./ --inline",
    "cy:run_spec": "npx cypress run --spec 'cypress/integration/End2EndTest.spec.js'",
    "cypress:run": "npm run cy:run_spec -- --browser electron",
    "CyTest": "npm run prereport && npm run cypress:run && npm run merge && npm run generateMocha_Report"
  },
  "author": "Abdullah",
  "license": "ISC",
  "dependencies": {
    "jasmine-core": "^3.10.1",
    "jasmine-spec-reporter": "^7.0.0",
    "junit": "^1.4.9",
    "junit-merge": "^2.0.0",
    "mocha": "^9.1.3",
    "mochawesome": "^7.0.1",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^6.0.1"
  },
  "devDependencies": {
    "cypress": "^8.5.0",
    "cypress-multi-reporters": "^1.5.0",
    "mocha-junit-reporter": "^2.0.2"
  }
}
